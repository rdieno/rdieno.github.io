import React from "react"

export default function InternalSectionGalleryRight({ children }) {
    return (
        <div className="internal-section-gallery-right">
            {children}
        </div>
    )
}