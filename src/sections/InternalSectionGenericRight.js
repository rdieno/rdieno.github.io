import React from "react"

export default function InternalSectionGenericRight({ children }) {
    return (
        <div className="internal-section-generic-right">
            {children}
        </div>
    )
}