import React from "react"

export default function InternalSectionGenericLeft({ children }) {
    return (
        <div className="internal-section-generic-left">
            {children}
        </div>
    )
}